@page
@model info.Pages.Infos.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Info</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Info.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Info.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Info.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Info.Description)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Info.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
